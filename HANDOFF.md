# HANDOFF — نقطه‌ی ورود پروژه‌ی هوای ایران

> **اگر سشن جدیدی هستی: این فایل را کامل بخوان، بعد `docs/WAR_LOG.md`. با این دو فایل سوار کاری.**
> آخرین به‌روزرسانی: ۱۷ شهریور ۱۴۰۵ (Claude Code — بعد از افزودنِ ساعتِ تقریبیِ بارش)
> وضعیت: **زنده و کار می‌کند، فیچرِ اصلی کامل است. منتظرِ فیدبکِ فرزین بعد از استفاده، هیچ بلوکه‌کننده‌ای نیست.**

---

## پروژه در یک پاراگراف

یک صفحه‌ی چت، فارسی، برای پرسیدنِ هوای شهرهای ایران با زبانِ محاوره‌ای («فردا رشت
بارون میاد؟»). بدونِ سرور، بدونِ کلید، بدونِ هزینه — کاملاً استاتیک روی GitHub Pages،
مستقیم از مرورگر به Open-Meteo وصل می‌شود. آدرسِ زنده:
**https://farzindna.github.io/weather-iran/**

## هدف واقعی مالک پروژه

فرزین اولش نقشه‌ی زنده‌ی ابر می‌خواست («دیدنِ ابرهایی که دارن میان حال می‌ده»)، ولی
بعد از یک دور تجربه با نسخه‌ی نقشه‌ای، صریح گفت نقشه را کلاً فراموش کنیم و چت به‌تنهایی
کافی است:

> «به نظرم اصلا نقشه رو دیگه نمیخوایم کلا فراموشش کن... سادگی و دقت از همه چیز
> مهم‌تره... فیچرهای عجیب و غریب نیاریم توش.»

⚠️ **پیامدش برای هر تصمیم:** هر پیشنهادی که سادگی را فدای یک قابلیتِ جذاب می‌کند،
رد می‌شود مگر دقت را هم واقعاً بالا ببرد (مثلِ سه‌مدلی‌کردنِ پیش‌بینی، که دقیقاً
همین آزمون را رد کرد).

## پروفایل مالک پروژه

- متخصصِ ژنتیک/بیوانفورماتیک، نه برنامه‌نویسِ حرفه‌ای — توضیح باید ساده و با
  تصویرِ عینی باشد («قبل: X، الان: Y»)، نه اصطلاحِ فنی خام.
- به دقتِ عددی حساس است: وقتی یک باگ («کوهدشت» تشخیص داده نمی‌شد، یا هوای یک شهرِ
  چینی به‌جایِ کوهدشت نشان داده شد) خودش را نشان داد، بلافاصله گزارشش داد و منتظرِ
  رفعِ ریشه‌ای ماند، نه فقط رفعِ همان مورد.
- به فرمتِ ادعا حساس است: از یک برچسبِ «AccuWeather-Style» که پشتش فقط هوای سالِ
  قبل بود، ناراضی بود — هر عددی که رو صفحه می‌آید باید واقعاً از همان منبعی باشد
  که ادعا می‌شود.
- می‌خواهد دیگران هم بتوانند از این ابزار استفاده کنند — به همین دلیل اسمِ شخصی
  از متنِ گفت‌وگو حذف شد.

---

## وضعیت فعلی

### چیزی که ساخته شده

| بخش | چیست |
|---|---|
| رابط | چتِ فارسی، تمِ تیره، فونتِ Vazirmatn |
| تشخیصِ زبان | بدونِ LLM — regex + لیستِ استاپ‌ورد فارسی (`assets/app.js`) |
| پیش‌بینیِ ≤ ۱۶ روز | میانگینِ سه مدلِ مستقل (ECMWF · GFS · ICON) از Open-Meteo، رایگان و بدونِ کلید |
| اطمینان | درصدِ توافقِ مدل‌ها (۰/۳۳/۶۷/۱۰۰٪) — نه عددِ ساختگی |
| ساعتِ بارش | جدید — اولین بلوکِ ساعتی که ≥۲ از ۳ مدل روش موافقند |
| پیش‌بینیِ > ۱۶ روز | صادقانه برچسب‌گذاری‌شده: هوای واقعیِ سالِ قبل در همین تاریخ، نه پیش‌بینی |
| شهرها | ~۵۰ شهرِ پرکاربرد هاردکد + geocoding آنلاینِ Open-Meteo، محدود به `country_code === 'IR'` |
| دیپلوی | GitHub Actions → GitHub Pages، تکی (بدونِ کپیِ دومِ `docs/`) |

### عددهای قطعی‌شده

- سقفِ نرخِ Open-Meteo: بیش از ۲ درخواستِ هم‌زمان → `429`.
- Geocodingِ آنلاین برای اسمِ فارسیِ شهرها درست کار می‌کند (کوهدشت را با نامِ
  فارسی هم پیدا می‌کند) — نیازی به رونویسیِ لاتین نیست.

---

## تصمیماتِ قفل‌شده (دوباره بحث نکن)

| # | تصمیم | دلیل |
|---|---|---|
| ۱ | نقشه‌ی زنده حذف شد، برنمی‌گردد | فرزین صریح خواست؛ چت به‌تنهایی کافی است |
| ۲ | هیچ کلیدِ AI/LLM در کد جاسازی نمی‌شود | سایتِ استاتیکِ عمومی بدونِ سرور = هر کلید در دیدرسِ همه از view-source؛ فقط راهِ معماریِ درست OAuth PKCE خودِ OpenRouter است (کاربر با اکانتِ خودش وصل می‌شود)، که یک قدمِ لاگین اضافه می‌کند — فعلاً ارزشش را ندارد |
| ۳ | میزبانی رو سرورِ genopars نه | هاستِ genopars اشتراکی‌ست (نه VPS)؛ جداییِ معماریِ واقعی ممکن نیست و ریسکِ کسب‌وکارِ پزشکیِ زنده را بالا می‌برد |
| ۴ | اعدادِ اطمینان همیشه از توافقِ واقعیِ مدل‌ها می‌آید | یک‌بار تجربه شد که «درصدِ احتمال» فقط یک آستانه‌ی ثابت بود، نه محاسبه — فرزین آن را رد کرد |
| ۵ | بدونِ اسمِ شخصی تو متنِ چت | فرزین می‌خواهد دیگران هم بتوانند استفاده کنند |

---

## قدم بعدی

هیچ بلوکه‌کننده‌ای نیست. آخرین درخواستِ اجراشده «ساعتِ تقریبیِ بارش» بود که کامل
شد و پوش شد. منتظرِ فیدبکِ فرزین بعد از استفاده‌ی واقعی.

### خطِ لوله — وضعیتِ واقعیِ اجرا

```
✅ چتِ فارسی + تشخیصِ شهر/تاریخ/قصد           → assets/app.js
✅ سه‌مدلیِ ECMWF/GFS/ICON با درصدِ توافق       → همان‌جا
✅ ساعتِ تقریبیِ شروعِ بارش                    → findRainWindow()
✅ برچسبِ صادقانه برای > ۱۶ روز                 → همان‌جا
✅ دیپلویِ تکی از روی گیت‌هاب (Actions)         → .github/workflows/
⬜ لایه‌ی AI/LLM                               → فقط ایده، تصمیمِ ۲ بالا را ببین
⬜ تبلیغِ کوچک برای سنجشِ استقبال               → فقط ایده، هنوز شروع نشده
```

---

## واقعیت‌های سختِ کشف‌شده

**۱. یک کلمه‌ی ناقص می‌تواند به یه شهرِ اشتباه گره بخورد.** «هوای کوهدشت چند درجه
است؟» باعث شد اپ به‌جایِ کوهدشت، هوای **Huaibei، چین** را نشان دهد — چون geocoding
آنلاینِ Open-Meteo کلمه‌ی «هوای» (که باید فیلتر می‌شد) را به‌عنوانِ اسمِ شهر گرفت و
فازی‌مچ کرد با یک شهرِ چینیِ هم‌ریشه. راه‌حلِ ریشه‌ای دو لایه است: (۱) لیستِ استاپ‌ورد
باید پسوندهای اضافه‌ی فارسی («ی» اضافه) را هم بشناسد، (۲) هر جواب geocoding که
`country_code !== 'IR'` باشد رد می‌شود — این لایه‌ی دوم مهم‌تر است چون از هر
کلمه‌ی ناشناخته‌ی بعدی هم محافظت می‌کند، نه فقط این یکی.

**۲. `direction: ltr` روی متنِ فارسی+رقمِ مخلوط، ترتیب را به‌هم می‌ریزد.** «۲ تا ۳»
با اجبارِ ltr می‌شود «۲ ۳ تا». برای متنِ RTL که رقمِ لاتین توش هست، جهتِ صفحه
(که خودش rtl است) را دست‌نخورده بگذار — مرورگر خودش رقم‌ها را درست می‌چیند.

**۳. بزرگ‌ترین ریسکِ باقی‌مانده:** هیچ. پروژه ساده و کم‌هزینه است — بدترین حالت،
Open-Meteo رایگان یک روز سقفِ نرخ را عوض کند.

---

## ساختار پوشه‌ها

```
04_weather-iran/
├── HANDOFF.md              ← همین فایل، نقطه‌ی ورود
├── AGENTS.md                ← برای Antigravity/ابزارِ دیگر
├── docs/WAR_LOG.md          ← باگ‌های گران و درس‌ها
├── index.html
├── assets/
│   ├── app.js               ← 🔵 تمامِ منطق: NLU، fetch، رندر
│   └── style.css
└── .github/workflows/       ← دیپلویِ خودکار به GitHub Pages
```
